describe("Bowling Game", function () {

	beforeEach(function () {
	});

	describe("", function () {
        
		it("", function () {
            //expect(berlinClock(new Date(2000, 1, 1, 0, 0, 0))).toMatch(/^Y/);
		});
	});
});