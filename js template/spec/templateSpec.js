describe("Template", function () {

	beforeEach(function () {
	});

	describe("the thing", function () {
        
		it("is empty right now", function () {
            expect(null).toBeNull();
		});
	});
});